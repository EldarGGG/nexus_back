[build]
builder = "nixpacks"
nixpacksConfigPath = "nixpacks.toml"

[deploy]
startCommand = "cd nexus_back && daphne -b 0.0.0.0 -p $PORT nexus_back.asgi:application"
healthcheckPath = "/api/health-check/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
